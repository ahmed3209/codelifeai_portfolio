# Copy this file to `.env.production` and fill in your deployed backend URL.
# This is baked into the build at `npm run build` time — change it, then rebuild.
# No trailing slash.
VITE_API_URL=https://your-backend.example.com
